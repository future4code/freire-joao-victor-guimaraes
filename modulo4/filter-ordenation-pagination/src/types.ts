export type recipe = {
   id: string
   title: string
   description: string
   userId: string
   createdAt: number
}

export type userTypes = {
   id: string
   name: string
   email: string
   type: string
   
}